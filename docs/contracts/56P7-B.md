# TASK CONTRACT — 56P7-B Client panel identity resolution hardening

## Objective
Fix `[mekvort_client_panel]` client resolution so authenticated WordPress client users reliably see their real Mekvort data.

## Scope
- audit current shortcode resolution flow
- add explicit client <-> WP user relation support (`wp_user_id`)
- resolve authenticated client primarily by `current_user_id()`
- add safe fallback by exact email match when relation is missing
- persist relation automatically when fallback resolves a single valid client

## In Scope
- client identity resolution pipeline used by client panel
- minimal persistence of relation metadata for future requests
- backward-compatible fallback for existing client records without relation
- concise documentation of fallback-safe migration behavior

## Out of Scope
- schema changes
- CRM/business logic redesign
- mechanic panel
- API changes
- legacy modules (`includes/modules/*`)
- unrelated UI redesign

## Allowed Files
- includes/dashboard/*
- includes/clients/*
- includes/portal/*
- includes/helpers/*
- includes/class-plugin.php (only if strict wiring is required)
- docs/CURRENT_STATE.md
- docs/tasks/*

## Forbidden Files
- includes/modules/*
- includes/database/*
- includes/crm/*
- REST/API namespaces not required by panel flow
- unrelated commercial/process modules

## Constraints
- respect `Controller -> Service -> Repository -> Database`
- no SQL outside repository/database layers
- no duplicated logic
- minimal scoped changes only
- do not break existing client records

## Deliverables
- exact cause documented
- `[mekvort_client_panel]` resolves authenticated client correctly
- fallback by exact email works only for unique valid match
- successful fallback persists relation for subsequent loads
- docs updated for safe fallback/migration behavior

## Validation
- `php scripts/php-lint.php --all`
- `php scripts/qa-runner.php --contract=docs/contracts/validation/56P7-B-validation.md --output=text`
- manual/runtime checks:
  - authenticated client with linked `wp_user_id` sees own data
  - authenticated client without link resolves by unique email and persists link
  - ambiguous/no-email-match does not auto-link incorrectly
  - panel sections load: dashboard, vehicles, processes, quotes, invoices (and documents/notifications when applicable)

## Validation Contract
- `docs/contracts/validation/56P7-B-validation.md`

## Status
PENDING
