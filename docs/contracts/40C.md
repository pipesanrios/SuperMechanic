PHASE: 40C
NAME: Metricas Operativas / SLA

scope:
  - extender Workload_Service con metricas operativas 40C
  - exponer metodo get_operational_metrics($business_id)
  - reutilizar datos y servicios existentes sin agregar infraestructura

allowed_files:
  - includes/dashboard/class-workload-service.php
  - docs/contracts/validation/40C-validation.md

forbidden_files:
  - includes/crm/*
  - includes/processes/*
  - includes/appointments/*
  - includes/database/*
  - scheduler/*

constraints:
  - no nuevas tablas
  - no cron
  - no recalculo historico complejo
  - no SQL fuera de Repository/Database
  - usar la misma politica de senales ya alineada con CRM Pipeline

expected_output:
  - metodo get_operational_metrics($business_id) con estructura:
    - tasks.avg_resolution_time
    - tasks.overdue_ratio
    - tasks.open_vs_closed
    - processes.avg_duration
    - processes.delayed
    - processes.completed_vs_active
    - alerts.critical
    - alerts.warning
    - appointments.completed_vs_scheduled
    - appointments.overdue

acceptance_criteria:
  - metricas calculadas con datos existentes
  - sin cambios de logica funcional en CRM/Processes/Appointments
  - tenancy respetada por business_id

validations_required:
  - php scripts/php-lint.php --all
  - php scripts/qa-runner.php --contract=docs/contracts/validation/40C-validation.md --output=text
  - validacion manual runtime de coherencia de metricas

validation_contract: docs/contracts/validation/40C-validation.md

docs_to_update:
  - none (implementacion tecnica 40C)

technical_debt:
  - optimizar metricas con cache si volumen historico crece
