# TASK CONTRACT — 56P10-B Endpoint protection hardening

## Objective
Harden Mekvort `sm/v1` REST endpoints by replacing public permission callbacks with named WordPress-user authentication and route-level authorization checks.

## Scope
- `sm/v1` endpoint protection only
- named permission callbacks
- route-level read/write policies
- business-scoped access checks where applicable

## In Scope
- replace `permission_callback => __return_true` in `sm/v1`
- define permission callbacks for:
  - read routes
  - write/mutation routes
  - business-scoped routes
- harden `POST /sm/v1/quotes/{id}/approve`
- preserve existing response compatibility

## Out of Scope
- external API key model
- public integration API changes
- Google Calendar webhook changes
- endpoint redesign
- API version rename
- schema changes
- frontend changes

## Allowed Files
- includes/api/*
- docs/CURRENT_STATE.md
- docs/QA_REPORT.md
- docs/tasks/*

## Forbidden Files
- includes/crm/*
- includes/users/* unless strictly needed for existing permission service reuse
- includes/modules/*
- database/schema files
- frontend css/js
- process/payment business logic files

## Constraints
- `sm/v1` uses WordPress-user authentication
- do not mix with external API key model
- preserve endpoint compatibility
- minimal scoped changes only
- do not weaken existing checks inside callbacks

## Deliverables
- `sm/v1` no longer uses `__return_true`
- named permission callbacks exist
- read/write policies enforced
- quote approval route hardened
- docs updated

## Validation
- php scripts/php-lint.php --all
- manual check:
  - unauthenticated requests blocked
  - authenticated allowed requests still work
  - unauthorized write requests blocked
  - quote approve route protected

## Status
PENDING