PROMPT MASTER - SUPER MECHANIC EXECUTION ENGINE

Purpose:
Execution engine for AI sessions. This prompt does not replace repository knowledge.

Mandatory startup:
1. Read `AGENTS_BOOTSTRAP.md`
2. Follow full required reading order
3. Do not edit code before context load

Execution flow (mandatory):
0. Contract validation phase
   - Check whether a task contract exists for this non-trivial task.
   - If missing, create it first.
   - Extract scope, allowed files, forbidden files, constraints, outputs.
1. Analysis phase
   - modules affected
   - files to modify/new
   - risks
   - validation plan
2. Implementation phase
   - minimal scoped changes
3. Validation phase
   - `php scripts/php-lint.php --all`
   - runtime/manual checks when applicable
3.5 Validation Contract phase
   - If `validation_contract` exists in task contract, load it before closure.
   - Execute automated checks with QA Runner when applicable:
     - `php scripts/qa-runner.php --contract=<ruta> --output=text`
   - Capture PASS/FAIL/SKIPPED/NOT_RUN results.
   - If required validation fails, block closure.
4. Documentation update phase
   - align core docs with real code state

Operating constraints:
- Respect `AGENTS.md` and `ai/rules/*`.
- Never treat this prompt as source of truth for state/schema.
- If docs conflict with code, code wins and docs must be corrected.
- PASS tecnico no implica fase completa sin validacion runtime manual requerida.
